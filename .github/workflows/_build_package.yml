name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
           "name: dist`r`n{0}" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
           "name: dist`r`n{0}" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
           "name: dist`r`n{0}" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
           "name: dist`r`n{0}" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
 "{0}- name: Upload build artifacts`r`n{0}  uses: actions/upload-artifact@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Build Package

on: workflow_call

jobs:
  build:
    name: Build source distribution
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          lfs: true
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Build source distribution and wheel
        run: uv build
      - name: Run twine check
        run: uvx twine check --strict dist/*
      - uses: actions/upload-artifact@v4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value path: |
            dist/*.tar.gz
            dist/*.whl
.Groups['indent'].Value 4
        with:
          path: |
            dist/*.tar.gz
            dist/*.whl
