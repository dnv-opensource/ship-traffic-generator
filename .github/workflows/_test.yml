name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        
        "python: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['version'].Captures.Value -join ', ') + "]`r`n"
        steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        
        "python: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['version'].Captures.Value -join ', ') + "]`r`n"
        steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            
        "python: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['version'].Captures.Value -join ', ') + "]`r`n"
        steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        
        "python: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['version'].Captures.Value -join ', ') + "]`r`n"
        steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        
        "python: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['version'].Captures.Value -join ', ') + "]`r`n"
        steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            
        "python: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        
        "os: [" + (name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
 "{0}- name: Checkout code`r`n{0}  uses: actions/checkout@v" -f name: Unit Tests

on: workflow_call

jobs:
  test:
    name: Test (Python ${{ matrix.python }}, ${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        platform:
          - runner: ubuntu-latest
          - runner: windows-latest
          # - runner: macos-latest
        python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['os'].Captures.Value -join ', ') + "]`r`n"
            python:
          - version: '3.11'
          - version: '3.12'
    steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['version'].Captures.Value -join ', ') + "]`r`n"
        steps:
      - uses: actions/checkout@v5
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
.Groups['indent'].Value 4
      - name: Install uv
        uses: astral-sh/setup-uv@v2
        with:
          enable-cache: true
          cache-dependency-glob: "uv.lock"
      - name: Install the project
        run: uv sync -p ${{ matrix.python }} -U --no-dev
      - name: Run pytest
        run: >
          uv run --with pytest --with pytest-cov
          pytest --cov
